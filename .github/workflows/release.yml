name: Release

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write

jobs:
  version:
    name: Version packages
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    outputs:
      has_changesets: ${{ steps.changesets.outputs.hasChangesets == 'true' }}
      pushed_tag: ${{ steps.push_tag.outputs.pushed == 'true' }}
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Create release pull request
        id: changesets
        uses: changesets/action@a45c4d594aa4e2c509dc14a9f2b3b67ba3780d0d # v1.9.0
        with:
          version: bun run version:packages
          commit: "chore: version packages"
          title: "chore: version packages"
          createGithubReleases: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Push release tag
        if: steps.changesets.outputs.hasChangesets == 'false'
        id: push_tag
        run: |
          VERSION="v$(bun -e 'import pkg from "./package.json"; console.log(pkg.version)')"
          if git ls-remote --tags origin "refs/tags/${VERSION}" | grep -q "${VERSION}"; then
            echo "Tag ${VERSION} already exists on origin"
            echo "pushed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          bun run release:tag
          git push origin --tags
          echo "pushed=true" >> "$GITHUB_OUTPUT"

  build:
    name: Build Windows release
    needs: version
    if: |
      always() && needs.version.result != 'failure' && (
        startsWith(github.ref, 'refs/tags/v') ||
        github.event_name == 'workflow_dispatch' ||
        (needs.version.outputs.pushed_tag == 'true' && needs.version.outputs.has_changesets != 'true')
      )
    runs-on: windows-latest
    env:
      PUBLISHED: ${{ (needs.version.outputs.pushed_tag == 'true' || github.event_name == 'workflow_dispatch') && '1' || '' }}
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          ref: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'main' }}

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22

      - name: Resolve release tag
        id: release
        run: bun run release tag --github-output

      - name: Install dependencies
        run: bun install

      - name: Typecheck
        run: bun run typecheck

      - name: Build release bundle
        run: bun run build

      - name: Zip release
        run: bun run release zip --tag ${{ steps.release.outputs.tag }}

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cursor-api-windows
          path: dist/cursor-api-*-win-x64.zip

      - name: Attach to GitHub Release
        if: steps.release.outputs.tag != ''
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: bun run release upload --tag ${{ steps.release.outputs.tag }}
